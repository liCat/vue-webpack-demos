###[返回根目录](https://github.com/liCat/vue-webpack-demos)
##step 8 多项目共用配置

在实际开发中，常会有多个项目，为每个项目配置build相关文件，显然效率太低。
但我们不难发现他们常常有明显的共同点，或者有一套约定俗成的项目树结构。
本demo基于demo7,但不再是单项目，而是多项目共用build文件。

同样，我们还是用optimist模块从命令行获取参数。

